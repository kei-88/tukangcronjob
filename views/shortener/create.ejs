<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener - Create Link</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Courier New', monospace;
      background: linear-gradient(135deg, #000814 0%, #001d3d 100%);
      color: #00d9ff;
      overflow: hidden;
    }

    /* Subtle grid background */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(0, 217, 255, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 217, 255, 0.03) 1px, transparent 1px);
      background-size: 50px 50px;
      pointer-events: none;
      opacity: 0.5;
    }

    /* Main container */
    .dashboard-container {
      display: flex;
      height: 100vh;
      position: relative;
      z-index: 1;
    }

    /* Sidebar */
    .sidebar {
      width: 280px;
      background: rgba(0, 8, 20, 0.9);
      border-right: 1px solid rgba(0, 217, 255, 0.3);
      display: flex;
      flex-direction: column;
      position: relative;
    }

    /* Sidebar glow effect */
    .sidebar::after {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      width: 1px;
      height: 100%;
      background: linear-gradient(180deg, 
        transparent 0%, 
        rgba(0, 217, 255, 0.5) 50%, 
        transparent 100%);
      animation: sidebarGlow 3s ease-in-out infinite;
    }

    @keyframes sidebarGlow {
      0%, 100% { opacity: 0.3; }
      50% { opacity: 1; }
    }

    /* Logo/Header */
    .sidebar-header {
      padding: 30px 20px;
      border-bottom: 1px solid rgba(0, 217, 255, 0.2);
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 8px;
    }

    .logo-icon {
      width: 40px;
      height: 40px;
      background: rgba(0, 217, 255, 0.1);
      border: 2px solid #00d9ff;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      font-weight: bold;
      box-shadow: 0 0 15px rgba(0, 217, 255, 0.3);
    }

    .logo-text {
      font-size: 20px;
      font-weight: bold;
      letter-spacing: 2px;
      text-shadow: 0 0 10px rgba(0, 217, 255, 0.5);
    }

    .logo-subtitle {
      font-size: 10px;
      color: rgba(0, 217, 255, 0.6);
      letter-spacing: 1px;
      padding-left: 52px;
    }

    /* Navigation */
    .nav-menu {
      flex: 1;
      padding: 20px;
      overflow-y: auto;
    }

    .nav-section {
      margin-bottom: 30px;
    }

    .nav-section-title {
      font-size: 11px;
      color: rgba(0, 217, 255, 0.5);
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 12px;
      padding-left: 15px;
    }

    .nav-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 15px;
      margin-bottom: 5px;
      color: rgba(0, 217, 255, 0.7);
      text-decoration: none;
      border-radius: 8px;
      border: 1px solid transparent;
      transition: all 0.3s;
      position: relative;
      overflow: hidden;
    }

    .nav-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      width: 3px;
      height: 100%;
      background: #00d9ff;
      transform: scaleY(0);
      transition: transform 0.3s;
    }

    .nav-item:hover {
      background: rgba(0, 217, 255, 0.05);
      border-color: rgba(0, 217, 255, 0.3);
      color: #00d9ff;
    }

    .nav-item.active {
      background: rgba(0, 217, 255, 0.1);
      border-color: rgba(0, 217, 255, 0.5);
      color: #00d9ff;
      box-shadow: 0 0 20px rgba(0, 217, 255, 0.2);
    }

    .nav-item.active::before {
      transform: scaleY(1);
    }

    .nav-icon {
      font-size: 18px;
    }

    .nav-badge {
      margin-left: auto;
      background: rgba(0, 217, 255, 0.2);
      color: #00d9ff;
      padding: 2px 8px;
      border-radius: 10px;
      font-size: 10px;
      border: 1px solid rgba(0, 217, 255, 0.3);
    }

    /* User section */
    .sidebar-footer {
      padding: 20px;
      border-top: 1px solid rgba(0, 217, 255, 0.2);
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px;
      background: rgba(0, 217, 255, 0.05);
      border: 1px solid rgba(0, 217, 255, 0.2);
      border-radius: 8px;
    }

    .user-avatar {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, #00d9ff, #0891b2);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      box-shadow: 0 0 15px rgba(0, 217, 255, 0.3);
    }

    .user-details {
      flex: 1;
    }

    .user-name {
      font-size: 14px;
      color: #00d9ff;
      margin-bottom: 2px;
    }

    .user-role {
      font-size: 10px;
      color: rgba(0, 217, 255, 0.6);
    }

    /* Main content */
    .main-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    /* Top bar */
    .topbar {
      background: rgba(0, 8, 20, 0.8);
      border-bottom: 1px solid rgba(0, 217, 255, 0.3);
      padding: 20px 30px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .page-title {
      font-size: 24px;
      color: #00d9ff;
      text-shadow: 0 0 10px rgba(0, 217, 255, 0.5);
    }

    .page-subtitle {
      font-size: 12px;
      color: rgba(0, 217, 255, 0.6);
      margin-top: 4px;
    }

    .topbar-actions {
      display: flex;
      gap: 15px;
      align-items: center;
    }

    .system-time {
      padding: 8px 15px;
      background: rgba(0, 217, 255, 0.05);
      border: 1px solid rgba(0, 217, 255, 0.2);
      border-radius: 6px;
      font-size: 12px;
    }

    /* Content area */
    .content-area {
      flex: 1;
      overflow-y: auto;
      padding: 30px;
    }

    /* Custom scrollbar */
    .content-area::-webkit-scrollbar {
      width: 8px;
    }

    .content-area::-webkit-scrollbar-track {
      background: rgba(0, 217, 255, 0.05);
    }

    .content-area::-webkit-scrollbar-thumb {
      background: rgba(0, 217, 255, 0.3);
      border-radius: 4px;
    }

    .content-area::-webkit-scrollbar-thumb:hover {
      background: rgba(0, 217, 255, 0.5);
    }

    /* Cards */
    .card {
      background: rgba(0, 8, 20, 0.6);
      border: 1px solid rgba(0, 217, 255, 0.3);
      border-radius: 12px;
      padding: 25px;
      margin-bottom: 25px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      position: relative;
      overflow: hidden;
    }

    .card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 2px;
      background: linear-gradient(90deg, transparent, #00d9ff, transparent);
      opacity: 0.5;
    }

    .card-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 20px;
    }

    .card-title {
      font-size: 18px;
      color: #00d9ff;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .card-title::before {
      content: '▸';
      color: #00d9ff;
      font-size: 20px;
    }

    /* Form elements */
    .form-group {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-bottom: 20px;
    }

    .form-label {
      font-size: 12px;
      color: rgba(0, 217, 255, 0.8);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .form-label::before {
      content: '$ ';
      color: #00d9ff;
    }

    .form-input {
      background: rgba(0, 20, 30, 0.6);
      border: 1px solid rgba(0, 217, 255, 0.3);
      color: #00d9ff;
      padding: 12px 15px;
      border-radius: 8px;
      font-family: 'Courier New', monospace;
      font-size: 14px;
      transition: all 0.3s;
    }

    .form-input:focus {
      outline: none;
      border-color: #00d9ff;
      box-shadow: 0 0 15px rgba(0, 217, 255, 0.3);
      background: rgba(0, 30, 40, 0.8);
    }

    .form-input::placeholder {
      color: rgba(0, 217, 255, 0.3);
    }

    /* Buttons */
    .btn {
      padding: 12px 25px;
      border: 2px solid #00d9ff;
      background: transparent;
      color: #00d9ff;
      font-family: 'Courier New', monospace;
      font-size: 14px;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s;
      position: relative;
      overflow: hidden;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: rgba(0, 217, 255, 0.2);
      transition: left 0.3s;
    }

    .btn:hover {
      background: rgba(0, 217, 255, 0.1);
      box-shadow: 0 0 20px rgba(0, 217, 255, 0.4);
      transform: translateY(-2px);
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn-primary {
      background: rgba(0, 217, 255, 0.1);
    }

    .btn-small {
      padding: 8px 15px;
      font-size: 12px;
    }

    .btn-danger {
      border-color: #ff4444;
      color: #ff4444;
    }

    .btn-danger:hover {
      background: rgba(255, 68, 68, 0.1);
      box-shadow: 0 0 20px rgba(255, 68, 68, 0.4);
    }

    /* Success/Error messages */
    .alert {
      padding: 15px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      border: 1px solid;
      font-size: 14px;
    }

    .alert-success {
      background: rgba(0, 255, 0, 0.1);
      border-color: rgba(0, 255, 0, 0.3);
      color: #00ff00;
    }

    .alert-error {
      background: rgba(255, 0, 0, 0.1);
      border-color: rgba(255, 0, 0, 0.3);
      color: #ff0000;
    }

    /* Result display */
    .result-card {
      background: rgba(0, 20, 30, 0.6);
      border: 1px solid rgba(0, 217, 255, 0.3);
      border-radius: 8px;
      padding: 20px;
      margin-top: 20px;
    }

    .result-title {
      font-size: 16px;
      color: #00d9ff;
      margin-bottom: 15px;
    }

    .result-url {
      font-family: 'Courier New', monospace;
      background: rgba(0, 0, 0, 0.3);
      padding: 10px;
      border-radius: 4px;
      word-break: break-all;
      margin-bottom: 10px;
    }

    .result-info {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
      font-size: 12px;
    }

    .result-info-item {
      display: flex;
      justify-content: space-between;
    }

    .result-info-label {
      color: rgba(0, 217, 255, 0.6);
    }

    .result-info-value {
      color: #00d9ff;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 11px;
      border: 1px solid;
    }

    .status-safe {
      background: rgba(0, 255, 0, 0.1);
      border-color: rgba(0, 255, 0, 0.3);
      color: #00ff00;
    }

    .status-blocked {
      background: rgba(255, 0, 0, 0.1);
      border-color: rgba(255, 0, 0, 0.3);
      color: #ff0000;
    }
  </style>
</head>
<body>

  <div class="dashboard-container">
    <!-- Sidebar -->
    <aside class="sidebar">
      <div class="sidebar-header">
        <div class="logo">
          <div class="logo-icon">LP</div>
          <div class="logo-text">LASTPIECE</div>
        </div>
        <div class="logo-subtitle">TEAM TOOLS PLATFORM</div>
      </div>

      <nav class="nav-menu">
        <div class="nav-section">
          <div class="nav-section-title">Tools</div>
          <a href="/dashboard" class="nav-item">
            <span class="nav-icon">⏰</span>
            <span>Cron Jobs</span>
          </a>
          <a href="/shortener" class="nav-item active">
            <span class="nav-icon">🔗</span>
            <span>URL Shortener</span>
          </a>
          <a href="/shortener/admin" class="nav-item">
            <span class="nav-icon">📊</span>
            <span>Admin Dashboard</span>
          </a>
        </div>
      </nav>

      <div class="sidebar-footer">
        <div class="user-info">
          <div class="user-avatar"><%= user.username.charAt(0).toUpperCase() %></div>
          <div class="user-details">
            <div class="user-name"><%= user.username %></div>
            <div class="user-role">Administrator</div>
          </div>
        </div>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <!-- Topbar -->
      <div class="topbar">
        <div>
          <div class="page-title">🔗 URL SHORTENER</div>
          <div class="page-subtitle">Create short links with Nawala detection</div>
        </div>
        <div class="topbar-actions">
          <div class="system-time" id="systemTime">00:00:00</div>
          <form action="/logout" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-small btn-danger">Logout</button>
          </form>
        </div>
      </div>

      <!-- Content -->
      <div class="content-area">
        <!-- Error/Success Messages -->
        <% if (error) { %>
          <div class="alert alert-error">
            <strong>Error:</strong> <%= error %>
          </div>
        <% } %>

        <% if (success) { %>
          <div class="alert alert-success">
            <strong>Success:</strong> <%= success %>
          </div>
        <% } %>

        <!-- Create Link Form -->
        <div class="card">
          <div class="card-header">
            <div class="card-title">Create Short Link</div>
          </div>
          
          <form action="/shortener/create" method="POST">
            <div class="form-group">
              <label class="form-label">Destination URL</label>
              <input
                type="url"
                name="destinationUrl"
                class="form-input" 
                placeholder="https://example.com/very-long-url"
                required
                value="<%= typeof destinationUrl !== 'undefined' ? destinationUrl : '' %>"
              />
            </div>
            <button type="submit" class="btn btn-primary">
              [[ Create Short Link ]]
            </button>
          </form>
        </div>

        <!-- Result Display -->
        <% if (shortLink && linkData) { %>
          <div class="card">
            <div class="card-header">
              <div class="card-title">Short Link Created</div>
            </div>
            
            <div class="result-card">
              <div class="result-title">Your Short Link:</div>
              <div class="result-url"><%= shortLink %></div>
              
              <div class="result-info">
                <div class="result-info-item">
                  <span class="result-info-label">Short Code:</span>
                  <span class="result-info-value"><%= linkData.shortCode %></span>
                </div>
                <div class="result-info-item">
                  <span class="result-info-label">Status:</span>
                  <span class="result-info-value">
                    <span class="status-badge <%= linkData.checkStatus === 'safe' ? 'status-safe' : 'status-blocked' %>">
                      <%= linkData.checkStatus === 'safe' ? 'Safe 🟢' : 'Blocked 🔴' %>
                    </span>
                  </span>
                </div>
                <div class="result-info-item">
                  <span class="result-info-label">Clicks:</span>
                  <span class="result-info-value"><%= linkData.clickCount %></span>
                </div>
                <div class="result-info-item">
                  <span class="result-info-label">Created:</span>
                  <span class="result-info-value"><%= new Date(linkData.createdAt).toLocaleString() %></span>
                </div>
              </div>
            </div>
          </div>
        <% } %>

      </div>
    </main>
  </div>

  <script>
    // Update system time
    function updateTime() {
      const now = new Date();
      const timeStr = now.toTimeString().split(' ')[0];
      document.getElementById('systemTime').textContent = timeStr;
    }
    updateTime();
    setInterval(updateTime, 1000);
  </script>

</body>
</html>
